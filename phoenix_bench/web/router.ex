defmodule PhoenixBench.Router do
  use PhoenixBench.Web, :router

  scope "/v1", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v2", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v3", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v4", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v5", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v6", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v7", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v8", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v9", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end

  scope "/v10", PhoenixBench do
    resources "/a/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d", Shared, only: [:index, :show, :create, :delete]
    resources "/a/b/c/d/ee/a/b/c/d/e", Shared, only: [:index, :show, :create, :delete]
  end


end
